{
  "name": "birthed-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static date pages for birthed.app. The search surface.",
  "engines": { "node": ">=18.18.0" },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js",
    "site": "npm run build && node dist/src/build.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
